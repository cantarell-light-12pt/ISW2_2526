<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Appenders>
        <Console name="ConsoleAppender" target="SYSTEM_OUT">
            <PatternLayout pattern="%highlight{%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n}{FATAL=red, ERROR=red, WARN=yellow, INFO=green, DEBUG=blue, TRACE=white}" />
        </Console>
    </Appenders>

    <Loggers>
        <!-- CK logs every source file it parses at INFO, which would bury the workflow logs under one
             line per class per release -->
        <Logger name="com.github.mauricioaniche.ck" level="WARN" />

        <Root level="INFO">
            <AppenderRef ref="ConsoleAppender" />
        </Root>
    </Loggers>
</Configuration>